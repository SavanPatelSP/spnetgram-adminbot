# End-to-End Flow Report — Admin ↔ Bot Synchronization

> Generated by live code audit of 6 critical flows.
> Every path verified by reading actual source files.

---

## Flow 1: Premium Request (User → Bot → Admin)

### Intended Path

```
Telegram User → /premium request → Bot → DB → Sync → SPNET-ADMIN
```

### Traced Path

| Step | File | Line | Status |
|------|------|------|--------|
| **Entry point (Bot)** | No `/premium` bot command exists | — | ❌ MISSING |
| **Entry point (API)** | `PremiumController.createSubscription()` | `premium.controller.ts:28` | ✅ WORKS |
| **API route** | `POST /api/premium/subscriptions` | `server.ts` (inline) | ✅ WORKS |
| **Service** | `PremiumService.createSubscription()` | `premium.service.ts:58` | ✅ WORKS |
| **Validation** | Checks user exists, checks plan exists, checks duplicate active sub | Lines 59-69 | ✅ WORKS |
| **Database table** | `UserSubscription` (Prisma model line 715) | `schema.prisma:715` | ✅ CREATED |
| **Event emitted (service)** | `PremiumEvents.subscriptionCreated()` | `premium.service.ts:87` | ✅ WORKS |
| **Event name** | `subscription:created` | `premium.events.ts:6` | ✅ WORKS |
| **Event consumed** | `SyncProcessor` registered consumer at line 69 | `sync.processor.ts:69` | ✅ WORKS |
| **Sync publisher** | `SyncPublishers.publishPremiumGranted()` | `premium.service.ts:88` | ✅ WORKS |
| **Publisher event** | `premium:granted` | `sync.publishers.ts:27` | ✅ WORKS |
| **Sync event created** | source=adminbot, target=spnetgram | `sync.processor.ts:134-141` | ✅ WORKS |
| **HTTP delivery** | `processEvent()` POST to `SPNET_ADMIN_API_URL` | `sync.processor.ts:240` | ✅ WORKS (if URL configured) |

### Gaps

| Gap | Impact | Root Cause |
|-----|--------|------------|
| No Telegram command to request premium | Users cannot self-request premium via bot | No premium bot command file exists |
| No `PremiumRequest` model | No structured "request" workflow | Only direct subscription creation exists |
| No approval chain for premium requests | All requests are immediately created | No integration with approval workflows |

### Verdict: PARTIALLY IMPLEMENTED

```
User → Bot:  ❌ MISSING (no bot command)
API → DB:     ✅ WORKS
DB → Sync:    ✅ WORKS
Sync → Admin: ✅ WORKS
```

---

## Flow 2: Grant Premium (Admin → Bot → User)

### Intended Path

```
SPNET-ADMIN → SyncEvent API → DB → SyncProcessor → InboundSyncService → BotNotificationDispatcher → Telegram User
```

### Traced Path

| Step | File | Line | Status |
|------|------|------|--------|
| **Admin posts event** | `POST /api/sync/events` (sync controller) | `sync.controller.ts:7` | ✅ WORKS |
| **DB write** | `SyncService.createEvent()` → source=spnetgram, target=adminbot | `sync.service.ts:12` | ✅ WORKS |
| **SyncProcessor picks up** | `processBatch()` → `processEvent()` | `sync.processor.ts:201-223` | ✅ WORKS |
| **Direction check** | `event.source === 'spnetgram' && event.target === 'adminbot'` | `sync.processor.ts:227` | ✅ WORKS |
| **InboundSyncService** | `processEvent()` called | `sync.processor.ts:228-229` | ✅ WORKS |
| **Validation** | Validates `premium:approved` requires `subscriptionId, userId, approvedBy` | `inbound-sync.service.ts:13` | ✅ WORKS |
| **BotNotificationDispatcher** | `dispatch('premium:approved', payload)` | `bot-notification-dispatcher.ts:20` | ✅ WORKS |
| **Notification type** | `PREMIUM_APPROVAL` | `bot-notification-dispatcher.ts:57` | ✅ WORKS |
| **Telegram channel** | `NotificationsService.create(channel='TELEGRAM')` | `bot-notification-dispatcher.ts:33-34` | ✅ WORKS |
| **Delivery retry** | 3 attempts with 1s, 2s, 4s backoff | `bot-notification-dispatcher.ts:135-150` | ✅ WORKS |
| **Fan-out** | Not needed for premium (has target userId) | — | N/A |

### Gaps

| Gap | Impact | Root Cause |
|-----|--------|------------|
| Admin must MANUALLY POST event | No automatic callback from SPNET-ADMIN to Bot when premium is granted | SPNET-ADMIN side not implemented yet |
| No `premium:extended` emitter | `publishPremiumExtended` was removed as dead code | Removed during cleanup (`sync.publishers.ts` history) |

### Verdict: PARTIALLY IMPLEMENTED

```
Admin posts event:  ✅ WORKS (API exists)
Validation:          ✅ WORKS
Notification build:  ✅ WORKS
Telegram delivery:   ✅ WORKS (with retry)
Automatic callback:  ❌ MISSING (needs SPNET-ADMIN webhook)
```

---

## Flow 3: Lifetime Conversion

### Intended Path

```
Admin → Convert Subscription → DB → Sync → Bot → Telegram User
```

### Traced Path

| Step | File | Line | Status |
|------|------|------|--------|
| **Sensitive action config** | `PREMIUM_LIFETIME` defined | `governance.sensitive-actions.ts:16` | ✅ CONFIG |
| **Seed data** | `Lifetime` plan with interval `'lifetime'` | `prisma/seed.ts:124` | ✅ SEED |
| **Service method** | No `convertToLifetime()` method exists | — | ❌ MISSING |
| **API route** | No route for lifetime conversion | — | ❌ MISSING |
| **Bot command** | No command for lifetime conversion | — | ❌ MISSING |
| **Sync publisher** | No `publishPremiumConvertedToLifetime` | — | ❌ MISSING |
| **Event contract** | No lifetime-specific event in contracts.ts | — | ❌ MISSING |

### Verdict: MISSING

```
Config:   ✅ EXISTS (sensitive action + seed)
Logic:    ❌ MISSING (no service method)
API:      ❌ MISSING (no route)
Bot:      ❌ MISSING (no command)
Sync:     ❌ MISSING (no event)
Telegram: ❌ MISSING (no notification)
```

---

## Flow 4: Ticket Creation

### Intended Path

```
User → Bot → DB → Sync → SPNET-ADMIN
```

### Traced Path

| Step | File | Line | Status |
|------|------|------|--------|
| **Entry point (Bot)** | `/tickets` lists only — NO create command | `tickets.ts:7` | ❌ LIST ONLY |
| **Entry point (API)** | `POST /api/tickets` | `server.ts` (inline) | ✅ WORKS |
| **Service** | `TicketsService.create()` | `tickets.service.ts:13` | ✅ WORKS |
| **DB table** | `Ticket` | `schema.prisma` | ✅ CREATED |
| **Event emitted** | `ticket:created` | `tickets.service.ts:31` | ✅ WORKS |
| **Event consumed** | `SyncProcessor` line 59 | `sync.processor.ts:59` | ✅ WORKS |
| **Sync publisher** | No `publishTicketCreated` (only `publishTicketClosed` exists) | `sync.publishers.ts:22` | ⚠️ PARTIAL |
| **Sync event** | Created via `onModuleEvent` → `createEvent()` | `sync.processor.ts:134-141` | ✅ WORKS |
| **HTTP delivery** | POST to SPNET-ADMIN | `sync.processor.ts:240` | ✅ WORKS |

### Gaps

| Gap | Impact | Root Cause |
|-----|--------|------------|
| No bot command to create tickets | Staff must use API instead of Telegram | `/tickets` is read-only |
| No `publishTicketCreated` publisher | `ticket:created` event still fires (emitted by service directly), but no dedicated publisher wrapper | Not critical (event fires directly) |

### Verdict: PARTIALLY IMPLEMENTED

```
Bot create:    ❌ MISSING (list only)
API create:    ✅ WORKS
DB create:     ✅ WORKS
Event emit:    ✅ WORKS (ticket:created)
Sync delivery: ✅ WORKS (via onModuleEvent)
```

---

## Flow 5: User Suspension

### Intended Path

```
Staff → /ban command → Bot → ModerationService → DB → Sync → SPNET-ADMIN
```

### Traced Path

| Step | File | Line | Status |
|------|------|------|--------|
| **Entry point (Bot)** | `/ban <user> <reason>` | `moderation.ts:77` | ✅ WORKS |
| **Entry point (API)** | `POST /api/moderation` | `server.ts` (inline) | ✅ WORKS |
| **Service** | `ModerationService.create()` | `moderation.service.ts:23` | ✅ WORKS |
| **DB table** | `ModerationAction` | `schema.prisma` | ✅ CREATED |
| **User status update** | `prisma.user.update({ status: BANNED })` | `moderation.service.ts:47-49` | ✅ WORKS |
| **Event emitted** | `moderation:action:created` + `moderation:action:executed` | `moderation.service.ts:57-58` | ✅ WORKS |
| **Event consumed** | `SyncProcessor` line 57 | `sync.processor.ts:57` | ✅ WORKS |
| **Sync delivery** | HTTP POST to SPNET-ADMIN | `sync.processor.ts:240` | ✅ WORKS |

### Gaps

| Gap | Impact | Root Cause |
|-----|--------|------------|
| `user:status:changed` NOT emitted | Admin panel doesn't see user status change event | `ModerationService` updates user status directly instead of calling `UsersService.updateStatus()` |
| No `/unban` bot command | Staff must use API to unban | Not registered |
| No `/unmute` bot command | Staff must use API to unmute | Not registered |

### Verdict: PARTIALLY IMPLEMENTED

```
Bot /ban:          ✅ WORKS
API:               ✅ WORKS
DB:                ✅ WORKS
Sync to admin:     ✅ WORKS (moderation events)
user:status:changed: ❌ NOT EMITTED (moderation bypasses UsersService)
/unban, /unmute:   ❌ MISSING
```

---

## Flow 6: Announcement Broadcast

### Intended Path

```
Admin → Broadcast Command → Bot → All Users (Telegram)
```

### Traced Path

| Step | File | Line | Status |
|------|------|------|--------|
| **Sensitive action config** | `GLOBAL_BROADCAST` defined | `governance.sensitive-actions.ts:30` | ✅ CONFIG |
| **Bot command** | No `/broadcast`, `/announce`, or similar | — | ❌ MISSING |
| **API route** | No broadcast endpoint | — | ❌ MISSING |
| **Service method** | No broadcast service | — | ❌ MISSING |
| **Fan-out** | `BotNotificationDispatcher.notifyExecutives()` exists but unused for broadcast | `bot-notification-dispatcher.ts:99` | ❌ NOT WIRED |
| **All-user broadcast** | No method to list all users and notify them | — | ❌ MISSING |

### Verdict: MISSING

```
Config:  ✅ EXISTS (sensitive action constant)
Command: ❌ MISSING (no /broadcast)
API:     ❌ MISSING (no route)
Service: ❌ MISSING (no logic)
Fan-out: ❌ MISSING (notifyExecutives exists but unused)
```

---

## Cross-Cutting Findings

### Additional Commands Listed in Guide but Not Registered

| Command | Listed in `/guide` | Registered | File |
|---------|-------------------|------------|------|
| `/lockdown` | Line 108 | ❌ NOT REGISTERED | — |
| `/liftlockdown` | Line 109 | ❌ NOT REGISTERED | — |
| `/grantpremium` | Guide mentions | ❌ NOT REGISTERED | — |
| `/creditcoins` | Guide examples | ❌ NOT REGISTERED | — |
| `/unmute` | `/commands` lists it | ❌ NOT REGISTERED | — |
| `/unban` | `/commands` lists it | ❌ NOT REGISTERED | — |

### Dead Events

| Event | In contracts.ts | Emitted by anyone | Notes |
|-------|----------------|-------------------|-------|
| `premium:extended` | Line 71; 184 | ❌ NEVER EMITTED | `publishPremiumExtended` was removed during cleanup |

### Processing Gaps

| Issue | Details |
|-------|---------|
| Moderation bypasses `user:status:changed` | `ModerationService.create()` updates `User.status` directly via Prisma at line 47-49 instead of calling `UsersService.updateStatus()`. The `user:status:changed` event is never emitted for suspensions. |
| No `publishTicketCreated` | `ticket:created` fires from the service directly (line 31) but there's no dedicated SyncPublishers wrapper. Not a functional gap since the event is still emitted and consumed by SyncProcessor. |

---

## Summary

### Working Flows

| Flow | End-to-End? | Notes |
|------|------------|-------|
| Ticket Creation (API) → Sync → Admin | ✅ | Full path works via API |
| User Suspension (Bot `/ban`) → Sync → Admin | ✅ | Full path works with `/ban` |
| Grant Premium (Admin event) → Bot → Telegram | ✅ | Requires admin to POST sync event |
| Premium Request (API) → Sync → Admin | ✅ | Full path via API only |

### Partially Implemented Flows

| Flow | Bot | API | Sync | Telegram | Notes |
|------|-----|-----|------|----------|-------|
| Premium Request | ❌ | ✅ | ✅ | ❌ | No user-facing bot command |
| Ticket Creation | ❌ (list only) | ✅ | ✅ | ❌ | No create command |
| Grant Premium (auto) | ✅ | ✅ | ✅ | ✅ | Needs SPNET-ADMIN callback |

### Missing Flows

| Flow | Config | Logic | API | Bot | Sync |
|------|--------|-------|-----|-----|------|
| Lifetime Conversion | ✅ seed | ❌ | ❌ | ❌ | ❌ |
| Announcement Broadcast | ✅ sensitive action | ❌ | ❌ | ❌ | ❌ |
| Security Lockdown (`/lockdown`) | ✅ LockdownService | ✅ exists | ❌ | ❌ Bot command | ✅ events |
| Premium Extended | ✅ contracts | ❌ removed | ❌ | ❌ | ❌ |

### Stub/Mock Flows

| Flow | What Exists | What's Missing |
|------|-------------|----------------|
| `GLOBAL_BROADCAST` | Config constant | Broadcast service, command, API route, fan-out logic |
| `PREMIUM_LIFETIME` | Config + seed plan | Conversion service method, command, API route, sync event |
| `publishPremiumExtended` | Contract definition | Emitter (was removed), service logic, bot notification |

### Production Blockers

| Blocker | SeverITY | Details |
|---------|----------|---------|
| No `/lockdown` / `/liftlockdown` commands | MEDIUM | Listed in guide as emergency actions but not registered; no way to trigger from Telegram |
| No `/unban` / `/unmute` commands | LOW | Listed in `/commands` but not registered |
| `user:status:changed` not emitted on ban | LOW | Moderation service bypasses UsersService; admin won't see user status change events for suspensions |
| `premium:extended` never emitted | LOW | Contract defined but emitter was removed; any listener expecting it will never fire |
| No premium bot commands | MEDIUM | Users cannot interact with premium features via Telegram at all |
| SPNET-ADMIN callback not implemented | MEDIUM | Admin→Bot events require manual API call from SPNET-ADMIN to bot |
